(function(){const products=window.DROPFORGE_PRODUCTS||[],store=window.DropForgeStore;const grid=document.querySelector('[data-products]');function render(){if(!grid)return;grid.innerHTML=products.map(p=>`<article class="product-card"><img loading="lazy" src="${p.image}" alt="${p.name}"><div class="product-meta"><small>${p.category} · ★ ${p.rating}</small><h3>${p.name}</h3><strong>$${p.price.toFixed(2)}</strong> <del>$${p.old.toFixed(2)}</del><button class="product-add" data-add="${p.id}">Add to cart</button></div></article>`).join('')}function drawer(){const items=store.cart(),box=document.querySelector('[data-cart-items]'),total=items.reduce((s,i)=>{const p=products.find(x=>x.id===i.id);return s+(p?p.price*i.qty:0)},0);document.querySelector('[data-cart-count]').textContent=items.reduce((s,i)=>s+i.qty,0);document.querySelector('[data-cart-total]').textContent='$'+total.toFixed(2);box.innerHTML=items.length?items.map(i=>{const p=products.find(x=>x.id===i.id);return `<div class="drawer-item"><img src="${p.image}" alt=""><div><strong>${p.name}</strong><span>${i.qty} × $${p.price.toFixed(2)}</span><button data-remove="${p.id}">Remove</button></div></div>`}).join(''):'<p style="color:#777;padding-top:25px">Your cart is empty.</p>'}function open(){document.querySelector('.cart-drawer').classList.add('open');document.querySelector('.drawer-backdrop').classList.add('open');drawer()}document.addEventListener('click',e=>{const add=e.target.closest('[data-add]');if(add){store.add(add.dataset.add);open()}const rem=e.target.closest('[data-remove]');if(rem){store.remove(rem.dataset.remove);drawer()}if(e.target.closest('[data-cart-open]'))open();if(e.target.closest('[data-cart-close]')){document.querySelector('.cart-drawer').classList.remove('open');document.querySelector('.drawer-backdrop').classList.remove('open')}});document.querySelector('[data-newsletter]')?.addEventListener('submit',e=>{e.preventDefault();alert('Thanks — you are on the DropForge list.')});render();drawer()})();
